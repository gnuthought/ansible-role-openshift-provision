---
- hosts: localhost
  connection: local
  roles:
  - role: openshift-provision
    openshift_clusters:
    - connection:
        server: https://openshift-master.libvirt
      login:
        username: userone
        password: password
      projects:
      - name: provision-test

        service_accounts:
        - name: testsa1
