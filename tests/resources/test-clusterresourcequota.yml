apiVersion: v1
kind: ClusterResourceQuota
metadata:
  name: test-clusterresourcequota
spec:
  quota:
    hard:
      pods: "10"
      secrets: "20"
  selector:
    annotations:
      openshift.io/requester: testuser
    labels: null
